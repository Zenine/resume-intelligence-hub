# OS
.DS_Store
Thumbs.db

# Editors / local tooling
.claude/
.idea/
.vscode/
*.swp
*.swo

# Node / package managers
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# Environment / local overrides
.env
.env.*
*.local

# Service account keys — GCP / GSC / any Google / cloud credential files
# (even if dropped into the repo dir by accident, git won't stage them)
*service-account*.json
*-service-account.json
gcp-key*.json
*.gcp.json
credentials.json
key.json

# Misc
*.tmp
*.log
